import 'package:flutter/material.dart';

class ProductCardHorizontalScreen extends StatelessWidget {
  const ProductCardHorizontalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}